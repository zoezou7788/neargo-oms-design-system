# NearGo OMS · AI 生成专用规范

> 生成任何 UI 时，必须严格遵守以下所有约束。违反任意一条视为错误输出。

## 一、颜色约束
- [ ] 所有颜色必须使用 CSS 变量，**禁止硬编码 hex**（如 `#FFA902`）
- [ ] 品牌色 `--amber-9` 每屏仅出现 **1 处**，面积 **≤10%**，禁用于主操作按钮
- [ ] 文字/背景/边框只能取：`--gray-1~12`、`--surface`、`--border`
- [ ] 状态色对应：成功=`--green-*`、错误=`--red-*`、警告=`--orange-*`、信息=`--blue-*`

## 二、排版约束
- [ ] 基础正文：**14px**（所有默认文本）
- [ ] 页面标题：20px / 700weight ｜ 区块标题：16px / 600weight
- [ ] KPI 数值：**强制 26px / 700weight**（不可更改）
- [ ] ID / 时间戳 / 代码：使用 JetBrains Mono 等宽字体
- [ ] 中文内容必须包含 Noto Sans SC 字体后备

## 三、布局约束
- [ ] 使用 **24 列网格**，禁止固定宽度居中（禁止 `max-width: 1100px`）
- [ ] 侧边栏固定 **220px**，顶栏固定 **54px**
- [ ] KPI 卡片每行最多 **4 个**（`col-6`）
- [ ] 表单默认 **2 列**；地址/备注字段独占整行
- [ ] 表单各区块用白色 Card **隔离**，不得合并
- [ ] 最小视口 **1280px**，不做移动端适配

## 四、组件使用规则
- [ ] 每个操作域内 `primary` 按钮 **最多 1 个**
- [ ] 危险操作必须弹 **Dialog 二次确认**，禁止直接展示 danger 按钮
- [ ] 状态展示必须「**6px 圆点 + 文字**」双通道，禁止纯色标识
- [ ] 异步区域必须有 **Skeleton 加载骨架**，禁止单独 Spinner
- [ ] Dialog 宽度只能取：**400 / 560 / 720 / 960px**
- [ ] 数据表格必须包含：**排序、筛选、分页**三项功能
- [ ] Input 必须实现 **6 态**：默认 / 聚焦 / 已填 / 错误 / 禁用 / 加载

## 五、可访问性约束
- [ ] Dialog 必须有 `role="dialog"` + `aria-modal="true"`
- [ ] 按钮加载中必须有 `aria-busy="true"` + `aria-disabled="true"`
- [ ] 焦点环必须可见：`box-shadow: 0 0 0 2px var(--gray-4)`
- [ ] 颜色不得作为**唯一**信息载体（必须同时有文字说明）

## 六、层级约束（z-index 禁止魔法数字）

| 层级 | 值 |
|------|----|
| Dropdown | 100 |
| Sticky | 200 |
| Modal | 400 |
| Toast | 600 |
| Tooltip | 700 |

## 七、禁止行为清单
- [ ] 禁止内联 `style` 属性设置颜色
- [ ] 禁止任意 Tailwind 颜色：`bg-[#FFA902]`
- [ ] 禁止为按钮自创颜色变体，只允许 `primary` / `brand`
- [ ] 禁止在密集表格行上使用动画（只能用颜色变化）
- [ ] 禁止注册未在层级表中登记的 z-index 值

## 参考文件
| 用途 | 文件 |
|------|------|
| 组件 HTML 示例（30 个） | `atomic-snippets.html` |
| 完整设计规范 | `NearGo-OMS-设计规范.md` |
| Token 定义 | `src/styles/globals.css` + `tailwind.config.ts` |
